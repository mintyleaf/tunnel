# tunnel (needs a better name)
slack's nebula wraparound to automate getting remote machine (llms, general rent) called from publically accessible server via http  
using nebula cre4ture pr with userspace port forwarding feature,
so at client side it can be launched in parallel with another docker container
inside the docker compose network, and forward needed ports to the control server


TBD
